# This project uses only public npm packages. Override the global ~/.npmrc
# (which points at https://npm.hubteam.com/ and 403s public packages, esp. on
# the bugspot VPN) so all installs go straight to public npm.
registry=https://registry.npmjs.org/
